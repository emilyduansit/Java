static members means whatever how many instances you create there is only one copy for the members and it is after the last modifying.

static method --- main function --static method can access the static members directly and other static methods.

static block, only one copy for different instances.